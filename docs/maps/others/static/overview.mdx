---
sidebar: staticapi
---
# Static API

Сервис Static API создаёт и показывает изображение статической карты <Brand /> на веб-страницах и в приложениях без использования JavaScript.

Изображение создаётся на основе параметров, указанных в URL: размера изображения, координат центра карты, масштаба карты, а также параметров дополнительных объектов (маркеров, линий и многоугольников).

Используйте Static API в следующих случаях:

* на сайтах для отображения превью карты;
* в приложениях, где не нужна интерактивность;
* на страницах, предназначенных для печати (например, листах доставки).

Попробуйте изменить размер изображения, координаты центра карты и её масштаб, чтобы получить нужный фрагмент карты и готовый запрос к Static API:

<HtmlExampleWithSrcCode iframeStyles={{ height: '630px' }}>
{`
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Static API</title>
    <meta name="description" content="Карта Static API" />
    <style>
        body {
            margin: 0;
            font-family: Arial;
        }

        .controls {
            display: flex;
            flex-direction: column;
            gap: 10px;
            padding: 10px;
            background: rgba(255, 255, 255, 0.9);
            border-radius: 8px;
            margin: 0px 0px;
        }

        .controls label {
            font-family: Arial;
            font-size: 14px;
            color: #6e6d6d;
        }

        .controls input {
            border: 1px solid #ccc;
            padding: 3px 7px;
            font-family: Arial;
            font-size: 14px;
            border-radius: 4px;
        }

        .controls button {
            padding: 6px 10px;
            font-family: Arial;
            font-size: 14px;
            border: none;
            border-radius: 5px;
            background: #0078FF;
            color: white;
            cursor: pointer;
            width: 180px;
        }

        .controls button:disabled {
            background: #f2f2f2;
            color: #6e6d6d;
            cursor: not-allowed;
        }

        .code {
            margin: 10px 0px;
            padding: 16px 20px;
            background: #fafafa;
            border: 1px solid #fafafa;
            border-radius: 0px;
            font-family: ptmono, monospace;
            font-size: 13px;
            color: #262626;
            max-width: 870px;
        }

        #map {
            display: block;
            margin: 0px 0px;
            overflow: hidden;
        }
    </style>
</head>

<body>
    <div class="controls">
        <label>
            Размер изображения:
            <input type="text" id="size" value="880x360">
        </label>
        <label>
            Координаты центра:
            <input type="text" id="center" value="55.768005,37.615655">
        </label>
        <label>
            Масштаб:
            <input type="number" id="zoom" value="13" min="1" max="18">
        </label>
        <button id="updateButton">Обновить карту</button>
    </div>
    <div class="code" id="generatedLink"></div>
    <img id="map" src="" alt="Карта Static API" crossorigin="anonymous">

    <script>
        const sizeInput = document.getElementById('size');
        const centerInput = document.getElementById('center');
        const zoomInput = document.getElementById('zoom');
        const updateButton = document.getElementById('updateButton');
        const mapImage = document.getElementById('map');
        const generatedLink = document.getElementById('generatedLink');

        const updateMap = () => {
            const size = sizeInput.value.trim();
            const center = centerInput.value.trim();
            const zoom = zoomInput.value.trim();

            if (!size || !center || !zoom) {
                alert('Заполните все поля');
                return;
            }

            const [width, height] = size.split('x').map(Number);

            if (width < 120 || width > 1280 || height < 90 || height > 1280 || zoom < 1 || zoom > 18) {
                alert('Проверьте значения: ширина 120-1280, высота 90-1280, масштаб 1-18');
                return;
            }

            const key = 'Your API access key';

            const mapUrl = \`https://static.maps.2gis.com/2.0?s=\${size}&c=\${center}&z=\${zoom}&key=\${key}\`;
            mapImage.src = mapUrl;
            generatedLink.textContent = \`https://static.maps.2gis.com/2.0?s=\${size}&c=\${center}&z=\${zoom}&key=API_KEY\`;
        };

        updateButton.addEventListener('click', updateMap);

        updateMap();
    </script>
</body>

</html>`}
</HtmlExampleWithSrcCode>

## Начало работы {#getting-started}

### 1. Получите ключ доступа {#get-access-key}

1. Зарегистрируйтесь в личном кабинете Менеджер Платформы.

   <CtaButton href="https://platform.2gis.ru/ru/?utm_source=docs&utm_medium=referral&utm_campaign=api_static" target="_blank">
      В кабинет
   </CtaButton>

2. Создайте [демо-ключ](/platform-manager/subscription/keys#demo) или купите [подписку](/platform-manager/subscription/purchase) для доступа к API. Подробнее о стоимости сервиса см. в разделе [Тарифы](#tariffs).

Подробнее о работе с ключами и подписками см. в [документации личного кабинета](/platform-manager/overview).

### 2. Разместите карту {#add-map}

Создайте ссылку (URL), добавьте в неё ваш API-ключ и разместите её в теге `<img>` на вашем сайте или в приложении. Вы можете разместить изображение карты в любом месте на сайте.

Например, следующая ссылка генерирует изображение карты c набором дополнительных объектов:

```bash
https://static.maps.2gis.com/2.0?s=880x450&z=14&pt=55.03652,82.91871&ls=55.03652,82.91871,55.02711,82.92131,55.02470,82.92698~w:5&pt=55.02470,82.92698~k:c~n:1&ls=55.03652,82.91871,55.03758,82.93058,55.04042,82.92996~c:ff0000&pt=55.04042,82.92996~k:c~n:2&pn=55.03687,82.89827,55.04067,82.90925,55.03636,82.91355,55.03264,82.91062,55.03489,82.90021~c:03ee10~f:03ee10a0&pt=55.03484,82.92290~k:c~c:pe&key=API_KEY
```

![Карта на Static API](/img/static/static_map_example.png)

## Тарифы и лимиты {#tariffs}

* Стоимость сервиса рассчитывается исходя из количества успешных запросов в месяц. Один запрос соответствует загрузке одного статического изображения карты.
* Для демо-ключей и ключей, созданных в рамках подписки, действуют лимиты на использование сервиса.
* Актуальную стоимость и лимиты см. в инструкции [Тарифы](/platform-manager/subscription/pricing).

## Варианты размещения {#distribution}

* **Облако**: Static API доступен через публичные endpoint-ы <Brand />.
* **On-Premise**: Static API недоступен при установке API-платформы <Brand /> в закрытом контуре.

## Помощь {#support}

* Если у вас возникли вопросы при работе с API, задайте их AI-ассистенту (в правом нижнем углу сайта), воспользуйтесь поиском по документации или отправьте электронное письмо на [api@2gis.ru](mailto:api@2gis.ru).
* Если вы хотите обсудить возможности API или его интеграцию с вашим продуктом, обратитесь к менеджеру.

   <CtaButton href="https://dev.2gis.ru/api/static-api" target="_blank" appearance="lightFill">
     Получить консультацию
   </CtaButton>

## Что дальше? {#whats-next}

* [Примеры использования](/maps/others/static/examples).
* [Справочник API](/maps/others/static/reference).
