---
sidebar: routingapi
---
# Обзор

Routing API позволяет строить на карте маршруты на транспорте (в том числе общественном) или пешком.

За один запрос вы можете построить:

* Для общественного транспорта: маршрут для одного набора точек на разных видах общественного транспорта. Для построения используйте endpoint [/public_transport/2.0](/api/navigation/routing/reference/public_transport).

   :::note[Статистика для общественного транспорта]

   Если для построения маршрутов на общественном транспорте вы ранее пользовались Public Transport API, предыдущая [статистика](/platform-manager/subscription/statistics) расходования лимитов и распределения запросов для Public Transport API сохранится.

   :::

* Для остальных способов передвижения:

   * маршрут для одного набора точек;
   * маршруты для нескольких пар начальных и конечных точек.
   
   Для построения используйте endpoint [/routing/7.0.0/global](/api/navigation/routing/reference/routing).

Если возможно, API строит несколько вариантов проезда по маршруту. Вы можете выбрать подходящий вариант маршрута, учитывая общее время в пути, расстояние, длину пешеходной части, а также другие параметры.

<CtaButton href="https://platform.2gis.ru/ru/?utm_source=docs&utm_medium=referral&utm_campaign=api_routing" target="_blank">
   В кабинет
</CtaButton>

## Пример работы {#example-usage}

Используйте интерактивный пример ниже, чтобы построить маршрут на автомобиле. Максимальная длина маршрута в примере — 50 км.

<HtmlExampleWithSrcCode iframeStyles={{ height: '500px' }}>
{`
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Routing API</title>
  <meta name="description" content="Построение маршрутов с помощью Routing API" />
  <style>
    html,
    body,
    #container {
      margin: 0;
      width: 100%;
      height: 100%;
      overflow: hidden;
    }

    .start-banner {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 300px;
      background: #F2F2F2;
      padding: 12px 16px;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
      text-align: center;
      z-index: 1000;
    }

    .controls {
      position: absolute;
      top: 10px;
      left: 10px;
      width: 240px;
      border-color: #D7D7D7;
      background: #FFFFFF;
      border-radius: 6px;
      padding: 8px 8px;
      z-index: 10;
    }

    .select-group {
      margin-top: 2px;
      margin-bottom: 12px;
    }

    .select-group label {
      font-family: 'Arial';
      font-size: 14px;
      color: #626262;
      padding: 0px 24px 8px 0px;
    }

    .select-group select {
      margin-top: 4px;
      border-color: #D7D7D7;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      width: 100%;
      -webkit-appearance: none;
      appearance: none;
      padding: 8px 24px 8px 8px;
      background:
        url("/img/drop-down.svg") no-repeat right 12px center,
        #F2F2F2;
      background-size: 14px 14px;
      box-sizing: border-box;
    }

    .select:active {
      background-color: #F2F2F2;
      color: #262626;
    }

    .select:hover {
      background-color: #F2F2F2;
      color: #0041C3;
    }

    .select:disabled {
      background-color: #F2F2F2;
      color: #929292;
      cursor: not-allowed;
    }

    .controls-button-calculate, .controls-button-reset {
      border: none;
      border-radius: 6px;
      padding: 8px 8px;
      font-family: 'Arial';
      font-size: 16px;
      display: block;
      width: 100%;
      box-sizing: border-box;
    }

    .select-group + .controls-button-calculate {
      margin-top: 20px;
    }

    .controls-button-calculate + .controls-button-reset {
      margin-top: 8px;
    }

    .controls-button-calculate:active, .controls-button-calculate:enabled {
      background-color: #0041C3;
      color: #E5E5E5;
    }

    .controls-button-reset:active, .controls-button-reset:enabled {
      background-color: #F2F2F2;
      color: #262626;
    }

    .controls-button-calculate:hover {
      background-color: #0028A6;
      color: #F2F2F2;
    }

    .controls-button-reset:hover {
      background-color: #E5E5E5;
      color: #0041C3;
    }

    .controls-button-calculate:disabled, .controls-button-reset:disabled {
      background-color: #E5E5E5;
      color: #929292;
      cursor: not-allowed;
    }

    .instructions-container {
      position: absolute;
      top: 80px;
      right: 50px;
      background: #FFFFFF;
      padding: 12px 16px;
      border-radius: 6px;
      width: 250px;
      height: 350px;
      z-index: 10;
      overflow-y: auto;
    }

    .instructions-container h3 {
      margin: 0 0 10px 0;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
    }

    .instructions-container p {
      margin: 0 0 8px 0;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
      background: #F2F2F2;
      padding: 12px 16px;
      border-radius: 6px;
    }

    .route-info-container {
      position: absolute;
      top: 10px;
      right: 50px;
      width: 250px;
      background: #FFFFFF;
      padding: 12px 16px;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
      text-align: left;
      z-index: 10;
      white-space: pre-line;
    }
  </style>
</head>

<body>
  <div id="container"></div>
  <div class="controls">
    <div class="select-group">
      <label for="transportSelect">Транспорт</label>
      <select id="transportSelect" name="transport">
        <option value="driving" selected>Автомобиль</option>
        <option value="taxi">Такси</option>
        <option value="bicycle">Велосипед</option>
        <option value="scooter">Самокат</option>
        <option value="motorcycle">Мотоцикл</option>
        <option value="truck">Грузовик</option>
        <option value="walking">Пешком</option>
      </select>
    </div>
    <button id="calculateButton" class="controls-button-calculate" disabled>Рассчитать маршрут</button>
    <button id="resetButton" class="controls-button-reset">Сбросить точки</button>
    <div class="tooltip" id="tooltip" style="display:none;"></div>
  </div>
  <div id="startBanner" class="start-banner">
    Отметьте три точки на карте
  </div>
  <script src="__MAPGL_API_URL__"></script>
  <script>
    const reqUrl = "https://routing.api.2gis.com/routing/7.0.0/global?key=Your directions API access key";

    const map = new mapgl.Map('container', {
      center: [37.615655, 55.768005],
      zoom: 11.5,
      key: 'Your API access key',
    });

    const startBanner = document.getElementById('startBanner');
    map.on('click', () => {
      if (startBanner) {
        startBanner.style.display = 'none';
      }
    });

    const markers = [];
    let firstPoint = null;
    let secondPoint = null;
    let thirdPoint = null;
    let routeLine = null;
    let selecting = 'a';

    const resetButton = document.getElementById('resetButton');
    const transportSelect = document.getElementById('transportSelect');
    const calculateButton = document.getElementById('calculateButton');
    const transportControls = document.querySelector('.controls');
    let transport = transportSelect.value;
    const transportFilters = [
      { id: 'btnDriving', transport: 'driving', filters: ["dirt_road", "toll_road", "ferry"] },
      { id: 'btnTaxi', transport: 'taxi', filters: ["dirt_road", "toll_road", "ferry"] },
      { id: 'btnBicycle', transport: 'bicycle', filters: ["ban_car_road", "ban_stairway"] },
      { id: 'btnScooter', transport: 'scooter', filters: ["ban_car_road", "ban_stairway"] },
      { id: 'btnMotorcycle', transport: 'motorcycle', filters: ["dirt_road", "toll_road", "ferry"] },
      { id: 'btnTruck', transport: 'truck', filters: ["dirt_road", "toll_road"] },
      { id: 'btnWalking', transport: 'walking', filters: ["dirt_road", "ferry", "highway"] },
    ];

    function removeElements(selectors) {
      selectors.forEach(selector => {
        const element = document.querySelector(selector);
        if (element) {
          element.remove();
        }
      });
    }

    function clearRouteLine() {
      if (routeLine) {
        routeLine.destroy();
        routeLine = null;
      }
    }

    resetButton.addEventListener('click', () => {
      selecting = 'a';
      firstPoint = null;
      secondPoint = null;
      thirdPoint = null;
      clearRouteLine();
      markers.forEach(m => m.destroy());
      markers.length = 0;
      transportSelect.disabled = false;
      calculateButton.disabled = true;
      routeLine = null;
      removeElements(['.instructions-container', '.route-info-container']);
      if (startBanner) startBanner.style.display = 'block';
    });

    map.on('click', (event) => {
      if (!event?.lngLat) return;
      const [lng, lat] = event.lngLat;

      if (selecting !== 'end') {
        let iconUrl;
        if (selecting === 'a') {
          iconUrl = 'https://disk.2gis.com/styles/assets/icons/start-2ec1e40c6a9f2b81815b42a4932012e3d6eda12e44b8591415424847c0f35260.svg';
        } else if (selecting === 'b') {
          iconUrl = 'https://disk.2gis.com/styles/assets/icons/interim_bus-fd37296da07136cf6815877ce3e1632b91571a77431cd79dc2d05b1d87a736ef.svg';
        } else if (selecting === 'c') {
          iconUrl = 'https://disk.2gis.com/styles/assets/icons/finish-22966aee51020ebd9f1a6c0a4a5e4333cb5f8a18fbffeeee693b7e18e6e5df78.svg';
        }

        const marker = new mapgl.Marker(map, {
          coordinates: [lng, lat],
          icon: iconUrl,
          zIndex: 1000,
        });
        markers.push(marker);
      }

      if (selecting === 'a') {
        firstPoint = { lon: lng, lat: lat };
        selecting = 'b';
      } else if (selecting === 'b') {
        secondPoint = { lon: lng, lat: lat };
        selecting = 'c';
      } else if (selecting === 'c') {
        thirdPoint = { lon: lng, lat: lat };
        selecting = 'end';
      }

      if (firstPoint && secondPoint && thirdPoint) {
        transportSelect.disabled = false;
        calculateButton.disabled = false;
        if (startBanner) startBanner.style.display = 'none';
      }
    });

    transportSelect.addEventListener('change', () => {
      transport = transportSelect.value;
    });

    calculateButton.addEventListener('click', () => {
      if (!(firstPoint && secondPoint && thirdPoint)) return;
      const filters = transportFilters[transport] || [];
      fetchRoute([firstPoint, secondPoint, thirdPoint], transport, filters);
    });

    transportFilters.forEach(({ id, transport, filters }) => {
      const btn = document.getElementById(id);
      if (!btn) return;
      btn.addEventListener('click', () => {
        fetchRoute([firstPoint, secondPoint, thirdPoint], transport, filters);
      });
    });

    function fetchRoute(points, transport, filters) {
      const pointTypes = {
        stop: ["driving", "taxi", "bicycle", "scooter", "motorcycle", "truck"],
        walking: ["walking"],
      };

      const startPointType = pointTypes.stop.includes(transport) ? "stop" : "walking";
      const endPointType = pointTypes.stop.includes(transport) ? "stop" : "walking";

      const formattedPoints = [
        { lon: points[0].lon, lat: points[0].lat, type: startPointType, start: true },
        { lon: points[1].lon, lat: points[1].lat, type: "pref" },
        { lon: points[2].lon, lat: points[2].lat, type: endPointType },
      ];

      const requestBody = {
        points: formattedPoints,
        locale: "ru",
        transport,
        filters,
        output: "detailed",
      };

      if (transport === "walking") {
        requestBody.params = {
          pedestrian: {
            use_instructions: true
          }
        };
      }

      fetch(reqUrl, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify(requestBody),
      })
        .then((res) => {
          if (!res.ok) {
            throw new Error('HTTP error! Status: ' + res.status);
          }
          return res.json();
        })
        .then((parsed) => {
          if (parsed.result && parsed.result.length > 0) {
            const totalDuration = parsed.result[0].ui_total_duration || 0;
            const totalDistanceValue = parsed.result[0].ui_total_distance.value || 0;
            const totalDistanceUnit = parsed.result[0].ui_total_distance.unit || 0;

            showRouteInfo(totalDuration, totalDistanceValue, totalDistanceUnit);

            const maneuvers = parsed.result[0].maneuvers;
            const coordinates = maneuvers.flatMap((maneuver) => {
              if (
                maneuver.outcoming_path &&
                maneuver.outcoming_path.geometry &&
                maneuver.outcoming_path.geometry.length > 0
              ) {
                return maneuver.outcoming_path.geometry
                  .flatMap((geometry) => {
                    const selection = geometry.selection;
                    return selection
                      .replace("LINESTRING(", "")
                      .replace(")", "")
                      .split(",")
                      .map((point) => point.trim().split(" ").map(Number));
                  });
              }
              return [];
            });

            if (coordinates.length > 0) {
              renderRoute(coordinates);
              renderManeuvers(maneuvers);
            } else {
              console.error("No coordinates found in response");
            }
          } else {
            console.error("No route found in response");
          }
        });
    }

    function showRouteInfo(duration, distanceValue, distanceUnit) {
      const routeInfoContainer = document.querySelector('.route-info-container') || document.createElement('div');
      routeInfoContainer.className = 'route-info-container';
      routeInfoContainer.innerHTML = 'Время в пути: ' + duration + '<br />Длина маршрута: ' + distanceValue + ' ' + distanceUnit;
      if (!routeInfoContainer.parentNode) {
        document.body.appendChild(routeInfoContainer);
      }
    }

    function renderManeuvers(maneuvers) {
      removeElements(['.instructions-container']);

      const instructionsContainer = document.createElement('div');
      instructionsContainer.className = 'instructions-container';

      const title = document.createElement('h3');
      title.textContent = 'Инструкции для маршрута';
      instructionsContainer.appendChild(title);

      maneuvers.forEach((maneuver, index) => {
        const instruction = document.createElement('p');
        let comment = maneuver.comment || '—';
        const outcomingPathComment = maneuver.outcoming_path_comment || '';
        if (comment === 'start') comment = 'Старт';
        if (comment === 'finish') comment = 'Финиш';
        instruction.textContent = (index + 1) + '. ' + comment + (outcomingPathComment ? ' (' + outcomingPathComment + ')' : '');
        instructionsContainer.appendChild(instruction);
      });

      document.body.appendChild(instructionsContainer);

      resetButton.onclick = () => {
        clearRouteLine();
        removeElements(['.instructions-container', '.route-info-container']);
        resetButton.textContent = 'Сбросить точки';
      };
    }

    function renderRoute(coordinates) {
      if (routeLine) {
        routeLine.destroy();
      }
      routeLine = new mapgl.Polyline(map, {
        coordinates,
        width: 6,
        color: "#0041C3",
      });
    }
  </script>
</body>

</html>`}
</HtmlExampleWithSrcCode>

Чтобы отредактировать код примера, нажмите **Исходный код** и затем **Codepen**.

:::tip[Песочница]

Вы также можете поработать с Routing API в [песочнице](https://platform.2gis.ru/ru/playground/routing/?utm_source=docs&utm_medium=referral&utm_campaign=api_routing) внутри личного кабинета (авторизация не требуется).

:::

## Основные возможности {#main-features}

### Способы передвижения {#transportation-types}

* автомобиль;
* такси;
* велосипед;
* самокат;
* мотоцикл;
* грузовой транспорт (есть [ограничения по территориям](#truck));
* экстренные службы;
* пешком (в том числе можно строить маршруты внутри зданий);
* общественный транспорт:

   * метро;
   * автобус;
   * трамвай;
   * троллейбус;
   * маршрутное такси;
   * пригородный поезд;
   * аэроэкспресс;
   * лёгкое метро;
   * монорельс;
   * фуникулёр;
   * речной транспорт;
   * канатная дорога;
   * легкорельсовый транспорт;
   * подземный трамвай;
   * Московское центральное кольцо;
   * Московские центральные диаметры.

### Параметры маршрута {#route-parameters}

При построении маршрутов вы можете указывать параметры в зависимости от способа передвижения и endpoint-а, к которому отправляется запрос.

#### Все способы, кроме общественного транспорта {#all-types}

В запросе к [/routing/7.0.0/global](/api/navigation/routing/reference/routing) вы можете указать:

* Координаты начальной и конечной точки, в том числе несколько пар.
* Промежуточные точки маршрута (не поддерживаются при построении маршрутов для нескольких пар начальных и конечных точек).
* [Способ передвижения](#transportation-types).
* Фильтры, исключающие типы дорог из маршрута.
* Фильтры, исключающие области на карте.
* Формат результата: полная выдача с геометрией маршрута или упрощённая выдача только со временем и длиной.
* Желаемое количество вариантов маршрута в ответе.
* Параметр для построения кратчайшего по времени или расстоянию маршрута.
* Параметр для учёта пробок: в реальном времени (с регулярным обновлением данных) или прогнозируемых на основе статистических данных.

   Пробки учитываются при расчёте маршрута, но детальные данные о пробках (например, цветовая индикация или уровень загруженности) в ответе API не предоставляются.

* Параметр для игнорирования перекрытий дорог.
* Направление движения для начальной или конечной точки.
* Дополнительно для грузового транспорта: массу и габариты транспортного средства, тип перевозимого груза, идентификаторы пропусков для въезда на территорию.
* Параметр для сохранения маршрута для дальнейшего использования.

В ответе вы можете получить:

* Несколько вариантов маршрута, если их возможно построить.
* Полную геометрию всех участков маршрута в текстовом формате WKT (Well-Known Text).
* Длину маршрута и время в пути: общую и для каждого участка.
* Инструкции по каждому участку маршрута: например, повернуть направо, продолжать движение прямо и т. д.
* Изменение высоты над уровнем моря на протяжении маршрута (для маршрутов на велосипеде, на самокате и пешком).
* Информацию о специальных точках на маршруте: например, о начале платного участка дороги.
* Информацию о стоимости платных участков дороги, если они есть на маршруте (для маршрутов на автомобиле и на такси).

Подробную информацию о параметрах см. в [Справочнике API](/api/navigation/routing/reference/routing).

#### Общественный транспорт {#public-transport}

В запросе к [/public_transport/2.0](/api/navigation/routing/reference/public_transport) вы можете указать:

* Время начала движения (отправления).
* Координаты начальной и конечной точки.
* Промежуточные точки маршрута.
* [Способ передвижения](#transportation-types).
* Параметр для учёта расписания общественного транспорта.
* Максимальное количество вариантов маршрута в ответе.
* Минимальное количество прямых маршрутов без пересадок в ответе.

В ответе вы можете получить:

* Несколько вариантов маршрута, если их возможно построить.
* Полную геометрию всех участков маршрута в текстовом формате WKT (Well-Known Text).
* Список маршрутов общественного транспорта и остановок.
* Расписание общественного транспорта и время прибытия на каждую остановку.
* Общую длину маршрута, длину пешеходной части и время в пути.
* Длину каждого участка маршрута, время в пути и время ожидания транспорта.
* Количество пересадок и список остановок для пересадки.

Подробную информацию о параметрах см. в [Справочнике API](/api/navigation/routing/reference/public_transport).

## Ограничения {#limitations}

### Точки маршрута {#points}

* В одном запросе вы можете указать:

    * Для одного набора точек с учётом начальной и конечной:

        * Для общественного транспорта: до 12 точек.
        * Для пешеходных маршрутов: до 5 точек.
        * Для остальных способов передвижения: до 10 точек.

    * Для нескольких пар точек: до 50 пар точек. При этом нельзя указать промежуточные точки. Доступно для всех способов передвижения, кроме общественного транспорта.

* При использовании демо-ключа максимальное расстояние между точками в маршруте — 50 км.

### Грузовой транспорт {#truck}

Построение маршрутов для грузового транспорта доступно в следующих странах:

* Азербайджан,
* Армения,
* Беларусь,
* Грузия,
* Казахстан,
* Кыргызстан,
* Россия,
* Узбекистан.

## Тарифы и лимиты {#tariffs}

* Стоимость сервиса рассчитывается одним из способов:

    * При построении маршрута для одного набора точек (начальные, конечные и промежуточные) оплачивается один построенный маршрут. Построение [вариантов маршрута](/api/navigation/routing/examples/options) в одном запросе дополнительно не оплачивается.
    * При построении [маршрутов для нескольких пар точек](/api/navigation/routing/examples/routing#multiple-routes) оплачивается маршрут для каждой пары. Например, если в одном запросе вы указываете пять пар точек (поле `points`), оплачиваются пять расчётов.

* Для демо-ключей и ключей, созданных в рамках подписки, действуют лимиты на использование сервиса. 
* Актуальную стоимость и лимиты см. в инструкции [Тарифы](/platform-manager/subscription/pricing).

## Варианты размещения {#distribution}

* **Облако**: все методы Routing API доступны через публичные endpoint-ы <Brand />.
* **On-Premise**: все методы Routing API доступны при установке API-платформы <Brand /> в закрытом контуре. Подробнее см. в разделе [API-платформа для сервера](/on-premise-api-platform/overview/summary).

## Помощь {#support}

* Если у вас возникли вопросы при работе с API, задайте их AI-ассистенту (в правом нижнем углу сайта), воспользуйтесь поиском по документации или отправьте электронное письмо на [api@2gis.ru](mailto:api@2gis.ru).
* Если вы хотите обсудить возможности API или его интеграцию с вашим продуктом, обратитесь к менеджеру.

   <CtaButton href="https://dev.2gis.ru/api/routing" target="_blank" appearance="lightFill">
     Получить консультацию
   </CtaButton>

## Что дальше? {#whats-next}

* [Начало работы](/api/navigation/routing/start).
* [Примеры](/api/navigation/routing/examples/overview) работы Routing API.
* [Справочник API](/api/navigation/routing/reference/routing).
