---
sidebar: tspapi
---
# Обзор

TSP API позволяет решить [задачу коммивояжёра](https://ru.wikipedia.org/wiki/Задача_коммивояжёра) (TSP/VRP): построить кратчайший по времени или расстоянию маршрут обхода указанных точек. Для обхода точек можно указать как одного курьера, так и нескольких. Если указано несколько курьеров, маршрут будет разделён между ними наиболее оптимальным способом (некоторые курьеры могут быть исключены, если использование меньшего числа курьеров будет оптимальнее).

Для маршрута можно указать до 4000 точек и до 200 курьеров.

:::tip

TSP API предлагает наиболее оптимальный *порядок* прохождения указанных точек с учётом всех особенностей дорожной ситуации. Чтобы получить детальный маршрут проезда или прохода, используйте [Routing API](/api/navigation/routing/overview) после получения решения от TSP API.

:::

## Пример работы {#example-usage}

<HtmlExampleWithSrcCode iframeStyles={{ height: '500px' }}>
{`
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>TSP API</title>
  <meta name="description" content="Решение задачи коммивояжёра с помощью TSP API" />
  <style>
    html,
    body,
    #container {
      margin: 0;
      width: 100%;
      height: 100%;
      overflow: hidden;
    }

    .start-banner {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 300px;
      background: #F2F2F2;
      padding: 12px 16px;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
      text-align: center;
      z-index: 1000;
    }

    .controls {
      position: absolute;
      top: 10px;
      left: 10px;
      width: 240px;
      border-color: #D7D7D7;
      background: #FFFFFF;
      border-radius: 6px;
      padding: 8px 8px;
      z-index: 10;
    }

    .select-group {
      margin-top: 2px;
      margin-bottom: 12px;
    }

    .select-group label {
      font-family: 'Arial';
      font-size: 14px;
      color: #626262;
      padding: 0px 24px 8px 0px;
    }

    .select-group select {
      margin-top: 4px;
      border-color: #D7D7D7;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      width: 100%;
      -webkit-appearance: none;
      appearance: none;
      padding: 8px 24px 8px 8px;
      background:
        url("/img/drop-down.svg") no-repeat right 12px center,
        #F2F2F2;
      background-size: 14px 14px;
      box-sizing: border-box;
    }

    .select:active {
      background-color: #F2F2F2;
      color: #262626;
    }

    .select:hover {
      background-color: #F2F2F2;
      color: #0041C3;
    }

    .select:disabled {
      background-color: #F2F2F2;
      color: #929292;
      cursor: not-allowed;
    }

    .controls-button-calculate, .controls-button-reset {
      border: none;
      border-radius: 6px;
      padding: 8px 8px;
      font-family: 'Arial';
      font-size: 16px;
      display: block;
      width: 100%;
      box-sizing: border-box;
    }

    .select-group + .controls-button-calculate {
      margin-top: 20px;
    }

    .controls-button-calculate + .controls-button-reset {
      margin-top: 8px;
    }

    .controls-button-calculate:active, .controls-button-calculate:enabled {
      background-color: #0041C3;
      color: #E5E5E5;
    }

    .controls-button-reset:active, .controls-button-reset:enabled {
      background-color: #F2F2F2;
      color: #262626;
    }

    .controls-button-calculate:hover {
      background-color: #0028A6;
      color: #F2F2F2;
    }

    .controls-button-reset:hover {
      background-color: #E5E5E5;
      color: #0041C3;
    }

    .controls-button-calculate:disabled, .controls-button-reset:disabled {
      background-color: #E5E5E5;
      color: #929292;
      cursor: not-allowed;
    }

    .loading-banner {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.5);
      color: #F2F2F2;
      justify-content: center;
      align-items: center;
      font-family: 'Arial';
      font-size: 20px;
      z-index: 1000;
      display: none;
    }

    .route-info-container {
      position: absolute;
      top: 10px;
      right: 50px;
      background: #FFFFFF;
      padding: 12px 16px;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
      text-align: left;
      z-index: 10;
      white-space: pre-line;
    }

    .tooltip {
      position: fixed;
      background: #FFFFFF;
      padding: 12px 16px;
      border-radius: 6px;
      font-family: 'Arial';
      font-size: 16px;
      color: #262626;
      text-align: left;
      z-index: 20;
      display: none;
      white-space: pre-line;
    }
  </style>
</head>

<body>
  <div id="container"></div>
  <div class="controls">
    <div class="select-group">
      <label for="transportSelect">Транспорт</label>
      <select id="transportSelect" name="transport">
        <option value="driving" selected>Автомобиль</option>
        <option value="walking">Пешком</option>
        <option value="bicycle">Велосипед</option>
        <option value="truck">Грузовик</option>
      </select>
    </div>
    <div class="select-group">
      <label for="courierCount">Курьеры</label>
      <select id="courierCount" name="courierCount">
        <option value="1">1 курьер</option>
        <option value="2">2 курьера</option>
      </select>
    </div>
    <button id="calculateButton" class="controls-button-calculate" disabled>Рассчитать маршрут</button>
    <button id="resetButton" class="controls-button-reset">Сбросить точки</button>
    <div class="tooltip" id="routeTooltip" style="display:none;"></div>
  </div>
  <div class="tooltip" id="tooltip"></div>
  <div class="loading-banner" id="loadingBanner">Построение маршрута...</div>
  <div id="startBanner" class="start-banner">
    Отметьте на карте по порядку одну точку склада и три точки доставки
  </div>
  <div class="route-info-container" id="routeInfoContainer" style="display: none;"></div>
  <script src="__MAPGL_API_URL__"></script>
  <script>
    const apiKey = 'Your directions API access key';
    const reqTaskUrl = 'https://routing.api.2gis.com/logistics/vrp/2.0/create?key=' + apiKey;
    const map = new mapgl.Map('container', {
      center: [37.615655, 55.768005],
      zoom: 11.5,
      key: 'Your API access key',
    });

    const startBanner = document.getElementById('startBanner');
    map.on('click', () => {
      if (startBanner) {
        startBanner.style.display = 'none';
      }
    });

    const resetButton = document.getElementById('resetButton');
    const transportSelect = document.getElementById('transportSelect');
    const courierCount = document.getElementById('courierCount');
    const calculateButton = document.getElementById('calculateButton');
    const tooltip = document.getElementById('tooltip');
    const loadingBanner = document.getElementById('loadingBanner');
    const routeInfoContainer = document.getElementById('routeInfoContainer');

    let warehouse = null;
    let deliveryPoints = [];
    let markers = [];
    let routeLines = [];

    resetButton.addEventListener('click', resetMap);

    map.on('click', (event) => {
      if (!event?.lngLat) return;
      const [lng, lat] = event.lngLat;

      if (!warehouse) {
        warehouse = { lon: lng, lat: lat };
        addMarker([lng, lat], 'https://disk.2gis.com/styles/assets/icons/poi_new_building-6a88ffa5acccc9b63228e14b105d21343fa5291eedfe2e0d1f48abd061e48982.svg');
      } else if (deliveryPoints.length < 3) {
        deliveryPoints.push({ lon: lng, lat: lat });
        addMarker([lng, lat], 'https://disk.2gis.com/styles/assets/icons/poi_mall-16265747925045068731b6f7e938269f4bfa52acce62204bef135c5f31b33c5b.svg');
      }

      if (warehouse && deliveryPoints.length === 3) {
        enableControls();
      } else {
        disableControls();
      }
    });

    calculateButton.addEventListener('click', () => {
      if (!warehouse || deliveryPoints.length < 3) {
        return;
      }
      showLoadingBanner();
      const transportType = transportSelect.value;
      const courierCountValue = parseInt(courierCount.value, 10);
      createTSPTask(transportType, courierCountValue);
    });

    function addMarker(coordinates, icon) {
      const markerOptions = {
        coordinates,
        icon,
        size: [30, 30],
        zIndex: 1000,
      };

      const marker = new mapgl.Marker(map, markerOptions);
      markers.push(marker);
    }

    function resetMap() {
      markers.forEach((marker) => marker.destroy());
      markers = [];
      routeLines.forEach((polyline) => polyline.destroy());
      routeLines = [];
      warehouse = null;
      deliveryPoints = [];
      disableControls();
      hideRouteInfo();

      const startBanner = document.getElementById('startBanner');
      if (startBanner) {
        startBanner.style.display = 'block';
      }
    }

    function enableControls() {
      transportSelect.disabled = false;
      courierCount.disabled = false;
      calculateButton.disabled = false;
    }

    function disableControls() {
      transportSelect.disabled = false;
      courierCount.disabled = false;
      calculateButton.disabled = true;
    }

    function showLoadingBanner() {
      loadingBanner.style.display = 'flex';
    }

    function hideLoadingBanner() {
      loadingBanner.style.display = 'none';
    }

    function showRouteInfo(duration, distance) {
      routeInfoContainer.style.display = 'block';
      routeInfoContainer.innerHTML =
        'Общее время в пути: ' + Math.round(duration / 60) + ' мин<br />' +
        'Общая длина маршрута: ' + (distance / 1000).toFixed(2) + ' км';
    }

    function hideRouteInfo() {
      routeInfoContainer.style.display = 'none';
      routeInfoContainer.textContent = '';
    }

    function getPollUrl(taskId) {
      return 'https://routing.api.2gis.com/logistics/vrp/2.0/status?task_id=' + taskId + '&key=' + apiKey;
    }

    function createTSPTask(transportType, courierCount) {
      const tspRequest = {
        waypoints: deliveryPoints.map((point, index) => {
          const wp = {
            id: String(index + 1),
            point: { lat: point.lat, lon: point.lon, type: "stop" },
            type: "delivery",
            service_duration_s: 600,
            depot_ids: ["0"],
            shipment_size: { weight_kg: 10 },
          };
          if (index === 0) {
            wp.time_windows = [{ time_window: "08:00-18:00", hard_time_window: "08:00-18:00" }];
          }
          return wp;
        }),
        agents: Array.from({ length: courierCount }, (_, i) => ({
          id: String(i + 1),
          capacity: { weight_kg: 500 },
          start_at: "0",
          finish_at: "0",
        })),
        depots: [{
          id: "0",
          point: { lat: warehouse.lat, lon: warehouse.lon, type: "stop" },
          service_duration_s: 0,
          time_window: {
            time_window: "08:00-18:00",
            hard_time_window: "08:00-18:00",
          },
        }],
        options: {
          routing_type: transportType,
          route_type: "jam",
          date: "2025-09-17",
          time_zone: "Europe/Moscow",
          solver_time_limit_s: 60,
        },
      };

      return fetch(reqTaskUrl, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
        },
        body: JSON.stringify(tspRequest),
      })
        .then((res) => {
          if (!res.ok) {
            throw new Error('API error: ' + res.status + ' ' + res.statusText);
          }
          return res.json();
        })
        .then((data) => {
          const taskId = data.task_id;
          if (!taskId) {
            throw new Error("No task_id in response");
          }
          pollTaskStatus(taskId);
        })
        .catch((error) => {
          hideLoadingBanner();
        });
    }

    function pollTaskStatus(taskId) {
      const reqPollUrl = getPollUrl(taskId);

      const interval = setInterval(() => {
        fetch(reqPollUrl, {
          method: "GET",
          headers: {
            "Content-Type": "application/json",
          },
        })
          .then((res) => res.json())
          .then((data) => {
            const status = data.status && data.status.status;
            if (status === 'Done' || status === 'Partial') {
              clearInterval(interval);
              const result = data.result;
              renderRoutes(result.routes);
              showRouteInfo(result.metrics.total_duration_s, result.metrics.total_distance_m);
              hideLoadingBanner();
            } else if (status === 'Fail') {
              clearInterval(interval);
              hideLoadingBanner();
            }
          })
          .catch((error) => {
            clearInterval(interval);
            hideLoadingBanner();
          });
      }, 5000);
    }

    function renderRoutes(routes) {
      routeLines.forEach((polyline) => polyline.destroy());
      routeLines = [];

      const tooltip = document.getElementById('tooltip');
      const colors = ["#0041C3", "#1CA91E"];
      let colorIndex = 0;

      routes.forEach((route) => {
        const coordinates = route.route
          .map((step) => {
            const node = step.node;
            if (node.type === 'depot' || node.type === 'agent') {
              return [warehouse.lon, warehouse.lat];
            }
            const wpId = parseInt(node.value.waypoint_id, 10);
            const point = deliveryPoints[wpId - 1];
            return point ? [point.lon, point.lat] : null;
          })
          .filter(Boolean);

        const dedupedCoords = coordinates.filter((coord, i) =>
          i === 0 || coord[0] !== coordinates[i - 1][0] || coord[1] !== coordinates[i - 1][1]
        );

        if (dedupedCoords.length < 2) return;

        const polyline = new mapgl.Polyline(map, {
          coordinates: dedupedCoords,
          color: colors[colorIndex % colors.length],
          width: 7,
        });

        polyline.on('mouseover', (event) => {
          tooltip.innerHTML = 'ID курьера: ' + route.agent_id + '<br />Время в пути: ' + Math.round(route.duration_s / 60) + ' мин<br />' + 'Длина маршрута: ' + (route.distance_m / 1000).toFixed(2) + ' км';
          tooltip.style.display = 'block';
          tooltip.style.left = (event.point[0] + 10) + 'px';
          tooltip.style.top = (event.point[1] + 10) + 'px';
        });

        polyline.on('mouseout', () => {
          tooltip.style.display = 'none';
        });

        routeLines.push(polyline);
        colorIndex++;
      });

      markers.forEach((marker, index) => {
        const pointId = index === 0 ? 0 : index;
        const point = index === 0 ? warehouse : deliveryPoints[index - 1];

        marker.on('mouseover', (event) => {
          tooltip.innerHTML = 'ID точки: ' + pointId + '<br />Координаты: [' + point.lon.toFixed(5) + ', ' + point.lat.toFixed(5) + ']';
          tooltip.style.display = 'block';
          tooltip.style.left = (event.point[0] + 10) + 'px';
          tooltip.style.top = (event.point[1] + 10) + 'px';
        });

        marker.on('mouseout', () => {
          tooltip.style.display = 'none';
        });
      });
    }
  </script>
</body>

</html>`}
</HtmlExampleWithSrcCode>

Чтобы отредактировать код примера, нажмите **Исходный код** и затем **Codepen**.

## Начало работы {#getting-started}

### 1. Получение ключа доступа {#getting-access-key}

1. Зарегистрируйтесь в личном кабинете Менеджер Платформы.

   <CtaButton href="https://platform.2gis.ru/ru/?utm_source=docs&utm_medium=referral&utm_campaign=api_tsp" target="_blank">
      В кабинет
   </CtaButton>

2. Создайте [демо-ключ](/platform-manager/subscription/keys#demo) или купите [подписку](/platform-manager/subscription/purchase) для доступа к API. Подробнее о стоимости сервиса см. в разделе [Тарифы](#tariffs).

Подробнее о работе с ключами и подписками см. в [документации личного кабинета](/platform-manager/overview).

### 2. Создание задачи {#create-task}

Примеры запросов ниже иллюстрируют задачу доставки груза со склада до трёх точек со следующими условиями:

* используется один курьер с грузоподъёмностью до 2000 кг;
* в каждую точку нужно доставить по 800 кг груза;
* курьер начинает и заканчивает путь на складе;
* склад и все точки доступны для посещения с 7:00 до 15:00.

TSP API решает задачу коммивояжёра в асинхронном режиме. Чтобы построить маршрут (порядок прохождения точек):

1. Отправьте запрос, чтобы создать задачу на построение маршрута.
2. Периодически проверяйте статус задачи, пока не завершится расчёт маршрута.
3. Получите готовый маршрут после завершения задачи.

---

Чтобы создать задачу на построение маршрута, отправьте POST-запрос на endpoint `/logistics/vrp/2.0/create`. В строке запроса укажите ваш ключ API в качестве значения параметра `key`:

```http
https://routing.api.2gis.com/logistics/vrp/2.0/create?key=API_KEY
```

Координаты точек маршрута, количество курьеров и другие параметры передайте в виде JSON в теле запроса. Например, чтобы задать параметры обхода из [примера задачи](#task-example):

1. Перечислите все точки для посещения в массиве `waypoints` и укажите параметры:

    * `id` (обязательный): идентификатор точки.
    * `point` (обязательный): координаты точки (`lat`, `lon`) и её расположение относительно дороги (`type`).
    * `type`: [тип точки](/api/navigation/tsp/examples#types) (`delivery`, `pickup` или `agent`).
    * `service_duration_s`: [продолжительность работы курьера в точке](/api/navigation/tsp/examples#service-time) (в секундах).
    * `depot_ids`: массив идентификаторов [складов](/api/navigation/tsp/examples#depots), с которых курьеры могут забрать заказ.
    * `time_windows`: [временные окна](/api/navigation/tsp/examples#time-windows).
    * `shipment_size`: размер груза.

   ```json
   {
       "waypoints": [
           {
            "id": "1",
            "point": { "lat": 55.708272, "lon": 37.46326, "type": "stop" },
            "type": "delivery",
            "service_duration_s": 0,
            "depot_ids": ["0"],
            "time_windows": [
                {
                    "time_window": "07:00-15:00",
                    "hard_time_window": "07:00-15:00"
                }
            ],
            "shipment_size": { "weight_kg": 800 }
            }
       ]
   }
   ```

2. Перечислите курьеров в массиве `agents` и укажите параметры:

    * `id` (обязательный): идентификатор курьера.
    * `capacity`: грузоподъёмность курьера.
    * `travel_time_multiplier`: коэффициент времени движения.
    * `cost`: стоимость работы курьера.
    * `start_at`: идентификатор склада, из которого курьер начинает маршрут.
    * `finish_at`: идентификатор склада, в который курьер возвращается.

   ```json
   {
    "agents": [
        {
            "id": "1",
            "capacity": { "weight_kg": 2000 },
            "travel_time_multiplier": 1.0,
            "cost": {
                "fixed": 3000,
                "hour": 100,
                "km": 8
            },
            "start_at": "0",
            "finish_at": "0"
        }
      ]
    }
   ```

3. (Опционально) Перечислите склады в массиве `depots`:

    * `id`: идентификатор склада.
    * `point`: координаты склада с указанием типа.
    * `service_duration_s`: время обслуживания на складе.
    * `time_window`: временное окно работы склада (`time_window`, `hard_time_window`).

    ```json
    {
      "depots": [
        {
            "id": "0",
            "point": {
                "lat": 55.734157,
                "lon": 37.589346,
                "type": "stop"
            },
            "service_duration_s": 0,
            "time_window": {
                "time_window": "07:00-15:00",
                "hard_time_window": "07:00-15:00"
             }
        }
      ]
    }
    ```

4. (Опционально) Укажите параметры маршрута в объекте `options`:

    * `route_type`: тип маршрута (например, `"jam"` — с учётом пробок).
    * `routing_type`: способ передвижения (например, `"driving"`).
    * `date`: дата маршрута в формате `YYYY-MM-DD`.
    * `time_zone`: часовой пояс (например, `"Europe/Moscow"`).
    * `solver_time_limit_s`: ограничение времени на решение задачи (в секундах).
    * `need_altitudes`: нужно ли учитывать высоты (`true` или `false`).

   ```json
   {
       "options": {
            "route_type": "jam",
            "routing_type": "driving",
            "date": "2021-02-19",
            "time_zone": "Europe/Moscow",
            "solver_time_limit_s": 60,
            "need_altitudes": false
        }
   }
   ```

#### Пример запроса {#request-example}

```bash
curl --location --request POST 'https://routing.api.2gis.com/logistics/vrp/2.0/create?key=API_KEY' \
--header 'Content-Type: application/json' \
--data '{
  "waypoints": [
    {
      "id": "1",
      "point": {
        "lat": 55.708272,
        "lon": 37.46326,
        "type": "stop"
      },
      "type": "delivery",
      "service_duration_s": 0,
      "depot_ids": [
        "0"
      ],
      "time_windows": [
        {
            "time_window": "07:00-15:00",
            "hard_time_window": "07:00-15:00"
       }
      ],
      "shipment_size": {
        "weight_kg": 800
      }
    },
    {
      "id": "2",
      "point": {
        "lat": 55.705352,
        "lon": 37.461495,
        "type": "stop"
      },
      "type": "delivery",
      "service_duration_s": 0,
      "depot_ids": [
        "0"
      ],
      "time_windows": [
        {
            "time_window": "07:00-15:00",
            "hard_time_window": "07:00-15:00"
        }
      ],
      "shipment_size": {
        "weight_kg": 800
      }
    },
    {
      "id": "3",
      "point": {
        "lat": 55.704175,
        "lon": 37.471526,
        "type": "stop"
      },
      "type": "delivery",
      "service_duration_s": 0,
      "depot_ids": [
        "0"
      ],
      "time_windows": [
        {
           "time_window": "07:00-15:00",
            "hard_time_window": "07:00-15:00"
        }
      ],
      "shipment_size": {
        "weight_kg": 800
      }
    }
  ],
  "agents": [
    {
      "id": "1",
      "capacity": {
        "weight_kg": 2000
      },
      "travel_time_multiplier": 1.0,
      "cost": {
        "fixed": 3000,
        "hour": 100,
        "km": 8
      },
      "start_at": "0",
      "finish_at": "0"
    }
  ],
  "depots": [
    {
      "id": "0",
      "point": {
        "lat": 55.734157,
        "lon": 37.589346,
        "type": "stop"
      },
      "service_duration_s": 0,
      "time_window": {
            "time_window": "07:00-15:00",
            "hard_time_window": "07:00-15:00"
      }
    }
  ],
  "options": {
    "route_type": "jam",
    "routing_type": "driving",
    "date": "2021-02-19",
    "time_zone": "Europe/Moscow",
    "solver_time_limit_s": 60,
    "need_altitudes": false
  }
}'
```

Запрос вернёт информацию о созданной задаче, включая её идентификатор (`task_id`), который нужно будет использовать для проверки статуса:

```json
{
    "task_id": "dcabb3f6188935187d54088556238b83",
    "status": {
        "status": "Run",
        "queued": "2026-04-20T11:44:14.906+00:00",
        "started": null,
        "matrix_downloaded": null,
        "calculated": null,
        "completed": null
    },
    "result": null
}
```

#### Ограничения {#limitations}

Запрос не будет выполнен, если: 

* начальная или конечная точка курьера была исключена из маршрута;
* время выполнения запроса не входит в рабочее время ни одного из курьеров.

### 3. Проверка статуса задачи {#check-task-status}

Чтобы проверить статус задачи, отправьте GET-запрос на endpoint `/logistics/vrp/2.0/status`. В строке запроса укажите два параметра:

* `task_id` — идентификатор задачи;
* `key` — ваш ключ API.

```bash
curl --location --request GET 'https://routing.api.2gis.com/logistics/vrp/2.0/status?task_id=dcabb3f6188935187d54088556238b83&key=API_KEY'
```

Если задача в процессе обработки, запрос вернёт тот же ответ, что и при создании задачи (поле `status` будет содержать значение `"Run"`):

```json
{
    "task_id": "dcabb3f6188935187d54088556238b83",
    "status": {
        "status": "Run",
        "queued": "2026-04-20T11:44:14.906+00:00",
        "started": null,
        "matrix_downloaded": null,
        "calculated": null,
        "completed": null
    },
    "result": null
}
```

Если задача завершена, тело ответа будет содержать следующие данные:

* `status`: одно из следующих значений:

  * `Done` — маршрут успешно построен.
  * `Partial` — маршрут построен, но из него были исключены точки или курьеры.
  * `Fail` — при построении маршрута возникла ошибка.

* `queued`: время постановки задачи в очередь в формате ISO 8601.

* `started`: время начала обработки.

* `matrix_downloaded`: время завершения загрузки матрицы достижимости (расстояний и времени в пути между всеми возможными парами точек из задачи).

* `calculated`: время завершения расчёта.

* `completed`: время завершения задачи. 

```json
{
    "task_id": "dcabb3f6188935187d54088556238b83",
     "status": {
        "status": "Done",
        "queued": "2026-04-20T11:44:14.906+00:00",
        "started": "2026-04-20T11:44:14.906+00:00",
        "matrix_downloaded": "2026-04-20T11:44:15.094+00:00",
        "calculated": "2026-04-20T11:44:15.144+00:00",
        "completed": "2026-04-20T11:44:15.144+00:00"
    }
}
```

### 4. Получение решения {#result}

Решение задачи содержится в блоках `result.metrics` и `result.routes`. Исключённые точки и курьеры содержатся в полях `result.dropped_waypoints` и `result.dropped_agents`. 

#### Пример ответа {#response-example}

```json
{
"result": {
    "metrics": {
        "number_of_routes": 2,
        "used_agents": 1,
        "dropped_waypoints_count": 0,
        "max_agent_runs": 2,
        "total_duration_s": 3897,
        "total_service_duration_s": 0,
        "total_early_count": 0,
        "total_early_duration_s": 0,
        "total_distance_m": 55501
    },
    "options": {
        "routing_type": "driving",
        "route_type": "jam",
        "date": "2021-02-19",
        "time_zone": "Europe/Moscow",
        "need_altitudes": false,
        "solver_time_limit_s": 60
    },
    "routes": [
        {
            "agent_id": "1",
            "run_number": 0,
            "duration_s": 931,
            "distance_m": 13606,
            "route": [
                {
                    "arrival_time": "2021-02-19T07:00:00+03:00",
                    "departure_time": "2021-02-19T07:00:00+03:00",
                    "node": {
                        "type": "depot",
                        "value": { "depot_id": "0", "delivery_to": "2" }
                    },
                    "service_start_time": "2021-02-19T07:00:00+03:00",
                    "waiting_duration_s": 0,
                    "transit_duration_s": 0,
                    "transit_distance_m": 0
                },
                {
                    "arrival_time": "2021-02-19T07:00:00+03:00",
                    "departure_time": "2021-02-19T07:00:00+03:00",
                    "node": {
                        "type": "depot",
                        "value": { "depot_id": "0", "delivery_to": "1" }
                    },
                    "service_start_time": "2021-02-19T07:00:00+03:00",
                    "waiting_duration_s": 0,
                    "transit_duration_s": 0,
                    "transit_distance_m": 0
                },
                {
                    "arrival_time": "2021-02-19T07:15:31+03:00",
                    "departure_time": "2021-02-19T07:15:31+03:00",
                    "node": {
                        "type": "waypoint",
                        "value": { "waypoint_id": "1", "picked_up_from": "0" }
                    },
                    "service_start_time": "2021-02-19T07:15:31+03:00",
                    "waiting_duration_s": 0,
                    "transit_duration_s": 931,
                    "transit_distance_m": 13606
                }
            ]
        },
        {
            "agent_id": "1",
            "run_number": 1,
            "duration_s": 2966,
            "distance_m": 41895,
            "route": [
                {
                    "arrival_time": "2021-02-19T07:30:16+03:00",
                    "departure_time": "2021-02-19T07:30:16+03:00",
                    "node": {
                        "type": "depot",
                        "value": { "depot_id": "0", "delivery_to": "3" }
                    },
                    "service_start_time": "2021-02-19T07:30:16+03:00",
                    "waiting_duration_s": 0,
                    "transit_duration_s": 885,
                    "transit_distance_m": 13546
                },
                {
                    "arrival_time": "2021-02-19T07:46:12+03:00",
                    "departure_time": "2021-02-19T07:46:12+03:00",
                    "node": {
                        "type": "waypoint",
                        "value": { "waypoint_id": "3", "picked_up_from": "0" }
                    },
                    "service_start_time": "2021-02-19T07:46:12+03:00",
                    "waiting_duration_s": 0,
                    "transit_duration_s": 956,
                    "transit_distance_m": 13441
                },
                {
                    "arrival_time": "2021-02-19T07:49:48+03:00",
                    "departure_time": "2021-02-19T07:49:48+03:00",
                    "node": {
                        "type": "waypoint",
                        "value": { "waypoint_id": "2", "picked_up_from": "0" }
                    },
                    "service_start_time": "2021-02-19T07:49:48+03:00",
                    "waiting_duration_s": 0,
                    "transit_duration_s": 216,
                    "transit_distance_m": 1376
                },
                {
                    "arrival_time": "2021-02-19T08:04:57+03:00",
                    "departure_time": "2021-02-19T08:04:57+03:00",
                    "node": {
                        "type": "agent",
                        "value": { "waypoint_id": "0" }
                    },
                    "service_start_time": "2021-02-19T08:04:57+03:00",
                    "waiting_duration_s": 0,
                    "transit_duration_s": 909,
                    "transit_distance_m": 13532
                }
            ]
        }
    ],
    "dropped_waypoints": [],
    "dropped_agents": []
    }
}
```

Подробнее о параметрах см. в [Справочнике API](/api/navigation/tsp/reference/create_200).

## Тарифы и лимиты {#tariffs}

* Стоимость сервиса рассчитывается исходя из объёма решаемой задачи: количества точек для посещения, умноженного на количество курьеров. Например, если в задаче 5 точек и 2 курьера, оплачиваются 10 расчётов.
* Для демо-ключей и ключей, созданных в рамках подписки, действуют лимиты на использование сервиса.
* Актуальную стоимость и лимиты см. в инструкции [Тарифы](/platform-manager/subscription/pricing).

## Варианты размещения {#distribution}

* **Облако**: все методы TSP API доступны через публичные endpoint-ы <Brand />.
* **On-Premise**: все методы TSP API доступны при установке API-платформы <Brand /> в закрытом контуре. Подробнее см. в разделе [API-платформа для сервера](/on-premise-api-platform/overview/summary).

## Помощь {#support}

* Если у вас возникли вопросы при работе с API, задайте их AI-ассистенту (в правом нижнем углу сайта), воспользуйтесь поиском по документации или отправьте электронное письмо на [api@2gis.ru](mailto:api@2gis.ru).
* Если вы хотите обсудить возможности API или его интеграцию с вашим продуктом, обратитесь к менеджеру.

   <CtaButton href="https://dev.2gis.ru/api/routing" target="_blank" appearance="lightFill">
     Получить консультацию
   </CtaButton>
